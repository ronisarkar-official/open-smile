interface:
  display_name: "Open Smile"
  short_description: "Repo-specific patterns and workflows for open-smile"
  default_prompt: "Use the open-smile repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true